name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build & test
    runs-on: macos-26
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      # Use the runner's default stable Xcode. Do not auto-select the "newest"
      # Xcode app because that can accidentally pick a release candidate.
      - name: Show Xcode toolchain
        run: |
          set -euxo pipefail
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcodebuild -version
          xcrun --sdk macosx --show-sdk-version
          swift --version

      # Avoid stale DerivedData / asset catalog state between runner image changes.
      - name: Clean Xcode state
        run: |
          rm -rf "$RUNNER_TEMP/DerivedData"
          rm -rf ~/Library/Developer/Xcode/DerivedData
          rm -rf ~/Library/Caches/com.apple.dt.Xcode

      # Build the app and run the frostTests unit suite. Code signing is disabled
      # because CI only needs to compile and run logic tests — it does not
      # produce a distributable, signed, notarized artifact (that stays local;
      # see RELEASING.md).
      - name: Build & test (no code signing)
        env:
          NSUnbufferedIO: YES
        run: |
          set -euxo pipefail

          DERIVED_DATA_PATH="$RUNNER_TEMP/DerivedData" \
          RESULT_BUNDLE_PATH="$RUNNER_TEMP/TestResults.xcresult" \
          scripts/test.sh

      # Also compile the Release configuration: the safety-critical auto-unlock
      # timer lives behind #if DEBUG, so Release-only breakage around that
      # boundary is invisible to a Debug-only test run.
      - name: Build Release configuration (no code signing)
        run: |
          set -euxo pipefail

          xcodebuild build \
            -project frost.xcodeproj \
            -scheme frost \
            -configuration Release \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM=""

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: TestResults
          path: ${{ runner.temp }}/TestResults.xcresult
          if-no-files-found: ignore
